# Multi Vendor E-Shop Frontend

This repository contains the frontend for the multi-vendor marketplace application.

It is configured for GitHub Pages deployment using the GitHub Actions workflow in `.github/workflows/deploy.yml`.
